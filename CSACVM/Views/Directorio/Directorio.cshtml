@using CSACVM.Modelos.ViewModels
@model CSACVM.Modelos.ViewModels.DirectorioVM

<div class="row">
    <div class="col-8 mt-3">
        Datatable Notas:
        <div class="row">
            <div class="card mt-3" id="datatableNotas">
                <div class="table-responsive overflow-hidden">
                    <table id="tablaNotas" class="table sizeFont stripe nowrap w-100">
                        <thead>
                            <tr>
                                <th></th>
                                <th></th>
                                <th></th>
                                <th></th>
                                <th></th>
                                <th></th>
                                <th></th>
                                <th></th>
                                <th></th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <div class="col-4 mt-3">
        Configuraciones:
        <div class="row mt-3 d-flex justify-content-center">
            <button class="btn btn-primary w-50" asp-controller="Directorio" asp-action="Notas">Añadir Nueva Nota</button>
        </div>
        <div class="row mt-3 d-flex justify-content-center">
            <button class="btn btn-primary w-50" asp-controller="Directorio" asp-action="Curriculum">Currículum Vitae</button>
        </div>

    </div>
</div>
<script>
    var notas = @Html.Raw(Json.Serialize(Model.ListaNotas));
    inicializarTablaNotas(notas);
</script>